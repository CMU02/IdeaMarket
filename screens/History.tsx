import { Text, View } from "react-native";
import styled from "styled-components";

const Container = styled(View)``;
const Title = styled(Text)``;

export default function History() {
  return (
    <Container>
      <Title>History 화면</Title>
    </Container>
  );
}
