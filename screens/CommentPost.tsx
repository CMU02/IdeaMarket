import { Text, View } from "react-native";
import styled from "styled-components";

const Container = styled(View)``;
const Title = styled(Text)``;

export default function CommentPost() {
  return (
    <Container>
      <Title>Comment Post 화면</Title>
    </Container>
  );
}
